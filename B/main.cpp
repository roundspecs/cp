#include <bits/stdc++.h>
#define fo(i, n) for (int i = 0; i < n; i++)
#define nn '\n'
#define fast                                                                   \
  ios_base::sync_with_stdio(0);                                                \
  cin.tie(0)
typedef long long ll;
using namespace std;

signed main() {
  fast;
  int t;
  cin >> t;
  while (t--) {
    
  done:
    cout << nn;
  }
}