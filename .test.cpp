#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#include "debug/debug.h"

int main() {
    cin.tie(0)->sync_with_stdio(0);
    int T;
    cin >> T;
    while (T--) {
        
    }
}