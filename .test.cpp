#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    cin.tie(0)->sync_with_stdio(0);
    int T;
    cin >> T;
    for(int tc = 1; tc <= T; tc++) {
        cout << "Case " << tc << ":\n";
        
    }
}