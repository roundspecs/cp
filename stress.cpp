#include <bits/stdc++.h>
using namespace std;

int main() {
  random_device rd;
  uniform_int_distribution<int> tc(1,10);
  uniform_int_distribution<int> INT(INT_MIN,INT_MAX);
  uniform_int_distribution<int> UINT(0,INT_MAX);
  int T=tc(rd);
  cout<<T<<"\n";
  while(T--) {

  }
}